/*
# API Reference
DisCal provides an extensive and powerful REST API for you to integrate into your website, discord bot, or application.
These docs provide a reference for the API and how to use it.

#### Base URL
`https://api.discalbot.com`

stuff

other stuff
other stuff
```
Something
something
```

## API Versioning
Some API versions are now non-functioning, and are labeled as discontinued in the table below for posterity. Trying to use these versions will fail and return an error.

You must specify the version number by including it in the request path like `https://api.discalbot.com/v{version_number}`.

##### API Versions
| Version | Status       | Default |
|---------|--------------|---------|
| 2       | Available    | ✓       |
| 1       | Discontinued |         |



import Markdown from "../../../components/markdown";
export default function ({ children }) {
    return <Markdown>{children}</Markdown>
}

 */

export default function Reference() {
    return <></>
}
